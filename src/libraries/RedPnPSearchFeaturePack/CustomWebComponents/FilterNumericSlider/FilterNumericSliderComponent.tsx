import * as React from "react";

export interface IFilterNumericSliderComponentProps {
}

const FilterNumericSliderComponent: React.FunctionComponent<IFilterNumericSliderComponentProps> = (props: IFilterNumericSliderComponentProps) : JSX.Element => {

    return (
        <>
            <span>-- RefinerNumericSliderComponent --</span>
        </>
    );
};

export default FilterNumericSliderComponent;